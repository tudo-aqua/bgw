{
  "$schema": "http://json-schema.org/draft-07/schema",
  "type": "object",
  "required": [
    "hostCards",
    "yourCards",
    "drawStack",
    "gameStack"
  ],
  "properties": {
    "hostCards": {
      "type": "array",
      "items": { "$ref": "#/$defs/card" }
    },
    "yourCards": {
      "type": "array",
      "items": { "$ref": "#/$defs/card" }
    },
    "drawStack": {
      "type": "array",
      "items": { "$ref": "#/$defs/card" }
    },
    "gameStack": { "$ref": "#/$defs/card" }
  },
  "additionalProperties": false,

  "$defs": {
    "card": {
      "type": "object",
      "required": [
        "suit",
        "value"
      ],
      "properties": {
        "suit": {
          "type": "string",
          "enum": ["C", "D", "H", "S"]
        },
        "value": {
          "type": "string",
          "enum": ["7", "8", "9", "10", "J", "Q", "K", "A"]
        }
      },
      "additionalProperties": false
    }
  }
}